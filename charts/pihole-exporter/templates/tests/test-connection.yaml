apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pihole-exporter.fullname" . }}-test-connection"
  labels:
{{ include "pihole-exporter.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pihole-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
